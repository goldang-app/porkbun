[project]
name = "porkbun-dns"
version = "0.1.0"
description = "Porkbun DNS record management tool"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "PyQt6>=6.6.0",
]
